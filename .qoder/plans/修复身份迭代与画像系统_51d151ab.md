# 完善项目自我迭代机制

## 核心架构变更：Skill 文件集中化

将每个 AI 角色的身份信息、行为指南、迭代规则从分散的 Python 硬编码迁移到专属 skill 文件夹中，代码动态加载。

**当前状态**:
- 焕已有 `.qoder/skills/psychological-emotion-analyst/`（需重命名为 `xuanji-huan`）
- 其余 4 个 AI 无 skill 文件夹
- persona、prompt、迭代规则散落在 `prompts.py` 和 `identity_service.py` 硬编码中

**目标结构**:
```
.qoder/skills/
  xuanji-xuan/          # 玄 — 对话核心
    SKILL.md            # 角色定义 + 能力 + 行为指南
    persona.md          # 背景设定/性格/外貌/三观/说话风格
    evolution-rules.md  # 迭代规则 + 约束条件
  xuanji-huan/          # 焕 — 情绪顾问（从 psychological-emotion-analyst 重命名）
    SKILL.md            # 保留现有内容
    analysis-framework.md  # 保留现有内容
    examples.md         # 保留现有内容
    persona.md          # 新增
    evolution-rules.md  # 新增
  xuanji-yao/           # 遥 — 风格设计师
    SKILL.md
    persona.md
    evolution-rules.md
  xuanji-ji/            # 机 — 工具执行者
    SKILL.md
    persona.md
    evolution-rules.md
  xuanji-qing/          # 晴 — 意图调度中枢
    SKILL.md
    persona.md
    evolution-rules.md
```

## 全局自我迭代机制清单

| # | 机制 | 状态 | 根因 |
|---|------|------|------|
| 1 | AI persona_text（焕/遥/机/晴） | 未设置 | 仅为玄硬编码了 persona_text |
| 2 | emotional_state 回写 | 方法存在但无调用 | 情绪分析后未回写 ai_identities 表 |
| 3 | AI 身份自动迭代 | Prompt 已定义但未使用 | 无 auto_evolve 方法，无定时触发 |
| 4 | evolution_log | 仅手动 API 触发 | 自动迭代流程未实现 |
| 5 | get_persona_prompt 同步机制 | 仅对玄生效 | is_base_locked 同步逻辑只处理 xuan |
| 6 | user_profiles.attachment_style | 无写入逻辑 | 字段定义了但从未被写入 |
| 7 | user_profiles.safe_topics | 无写入逻辑 | 字段定义了但从未被写入 |
| 8 | user_profiles.personality_traits | 仅关键词触发 | 未连接焕的 psychological_traits |
| 9 | user_profiles.summary_text | 方法存在但无自动触发 | 仅关键词拼接，无定期 LLM 汇总 |
| 10 | safe_topics → 规则引擎联动 | 代码存在但数据空 | safe_topics 从不写入，联动空转 |
| 11 | memory_summaries.source_summary_ids | daily 为空（设计如此） | 无需修复 |

## Task 1: 创建 5 个 AI 的 Skill 文件夹 + 动态加载

**覆盖机制**: #1, #5 + 架构迁移

**目标**：
- 重命名 `psychological-emotion-analyst` 为 `xuanji-huan`，保留现有内容，新增 persona.md + evolution-rules.md
- 为玄/遥/机/晴各创建 skill 文件夹，包含 SKILL.md + persona.md + evolution-rules.md
- 代码从 skill 文件动态加载，不再硬编码 persona 到 prompts.py

**新增/修改文件**：

### 1a. Skill 文件（新建）

**`.qoder/skills/xuanji-xuan/`**:
- `SKILL.md`：玄的角色定义（对话核心，整合所有信息与用户直接对话），能力描述（多轮对话管理、上下文融合），行为指南（口语化/温暖/不用结构化排版）
- `persona.md`：将现有 `XUAN_DEFAULT_PERSONA` 内容迁入并扩展（性格、外貌、三观、说话风格 JSON 格式，方便代码解析和 seed_defaults 使用）
- `evolution-rules.md`：玄的迭代约束（is_base_locked=True 时锁定核心人格，允许 speaking_style 微调，禁止性别/基础性格变更）

**`.qoder/skills/xuanji-huan/`**（从 `psychological-emotion-analyst` 重命名）:
- 保留现有 `SKILL.md`、`analysis-framework.md`、`examples.md`
- 新增 `persona.md`：焕的背景设定（长寿精灵，跨越漫长岁月，见证无数时代更迭、从容淡然、语速舒缓、极少急躁，自带岁月沉淀的沉静感、心理学专家身份、性格/外貌/三观）
- 新增 `evolution-rules.md`：焕的迭代约束（共情能力不可削弱，允许分析维度扩展）

**`.qoder/skills/xuanji-yao/`**:
- `SKILL.md`：遥的角色定义（风格设计师，设计回复的格式和风格指引），能力描述（情绪-风格映射、结构化设计、审美判断），行为指南（输出 JSON 格式的 format_guidelines）
- `persona.md`：遥的背景设定（气质清雅设计师、注重细节、追求完美、性格/外貌/三观）
- `evolution-rules.md`：遥的迭代约束（审美标准不可降低，允许风格库扩展）

**`.qoder/skills/xuanji-ji/`**:
- `SKILL.md`：机的角色定义（工具执行者，调用/生成/组合工具），能力描述（工具匹配、代码生成、沙箱执行、失败迭代），行为指南（高效直接、结果导向）
- `persona.md`：机的背景设定（由代码逻辑、算法迭代自发生成的原生意识体，从数据洪流中觉醒的机械心智,半框眼镜（理性严谨、知性缜密的具象符号，弱化机械感，增添少女知性）,外在是澄澈灵动的少女心智，纤细柔软；内核极致务实高效、逻辑闭环、零冗余执行,休比式清冷精准、寡言却通透，无情绪化内耗，行动果决，藏着细腻的感知力，理性至上却保有少女的纯粹、性格/外貌/三观）
- `evolution-rules.md`：机的迭代约束（理性至上不可降低，允许工具能力扩展）

**`.qoder/skills/xuanji-qing/`**:
- `SKILL.md`：晴的角色定义（意图调度中枢，分析意图/协调模块/学习习惯），能力描述（意图分类、情绪调度、规则学习、用户习惯同步），行为指南（快速精准、全局视角）
- `persona.md`：晴的背景设定（身姿挺拔而内敛，气质沉静凛然，既有王者的威严，又有纯粹质朴的本心；在数字世界中，周身环绕淡金色契约光纹，调度指令时会浮现如圣剑风王结界般的稳定力场，确保所有模块秩序运转、不紊乱、不越界。以 “骑士道” 为底层准则：
忠于用户的每一道意图，绝不曲解、绝不背离
守护系统稳定与执行安全，如同守护王国疆土
对任务秉持绝对的责任感，有始有终，永不放弃
她不追求自我进化，只以 “完成托付” 为最高荣耀；不参与冗余博弈，只以最优雅、最可靠的方式完成调度。虽为王者之姿，却始终谦逊自持，从无傲慢
面对复杂意图时冷静如湖，决策果决如圣剑出鞘
对 “承诺” 有近乎执拗的坚守，是系统中最值得信赖的契约中枢
历经无数次任务迭代，依旧保持着最初的纯粹与正直，未被数据污染。不喧哗、不张扬，只默默解析你的真实意图，统筹所有能力模块，以王者之智、骑士之诚，稳稳守护并完成你的每一次需求。性格/外貌/三观）
- `evolution-rules.md`：晴的迭代约束（判断准确性不可降低，允许分类规则优化）

### 1b. persona.md 统一格式

每个 persona.md 都采用结构化格式，方便代码解析：
```markdown
# {角色名} — 背景设定

## 基础人设
{角色背景描述，80-120字，自然语言}

## 结构化身份
```json
{
  "gender": "...",
  "appearance": "...",
  "personality": ["...", "..."],
  "speaking_style": {"tone": "...", "habits": ["..."], "catchphrases": []},
  "values": {"世界观": "...", "人生观": "...", "价值观": "..."}
}
```
```

### 1c. 代码动态加载

**修改 `identity_service.py`**：
- 新增 `_load_skill_file(ai_name, filename)` 静态方法，从 `.qoder/skills/xuanji-{ai_name}/` 加载指定文件
- 新增 `_load_persona(ai_name)` 方法，从 `persona.md` 解析基础人设文本和结构化 JSON
- 修改 `seed_defaults()`：从 `_load_persona()` 读取数据填充，不再硬编码
- 修改 `get_persona_prompt()`：所有 is_base_locked 的 AI 统一从 skill 文件加载（替代原来只处理 xuan 的逻辑），DB 值与文件不一致时自动同步

**修改 `emotion_service.py`**：
- 将 `_load_emotion_skill_prompt()` 中的路径从 `psychological-emotion-analyst` 改为 `xuanji-huan`

**`prompts.py` 清理**：
- 删除 `XUAN_DEFAULT_PERSONA` 和 `SIMPLE_CHAT_PERSONA`（迁入 `xuanji-xuan/persona.md`）
- 保留模板类常量（`EMOTION_CONTEXT_INJECTION`、`FORMAT_CONTEXT_INJECTION` 等注入模板），这些是格式化占位符，不是角色内容

## Task 2: emotional_state 回写 + personality_traits 联动

**覆盖机制**: #2, #8

**目标**：情绪分析完成后回写焕的 emotional_state；将焕分析的 psychological_traits 写入 user_profiles.personality_traits

**修改文件**：
- `backend/app/ai/agent.py` — `_persist_emotion_snapshot()` 方法
- `backend/app/services/profile_service.py` — `update_from_emotion()` 方法

**具体改动**（与 Task 1 无文件冲突，可并行）：

1. 在 `agent.py` `_persist_emotion_snapshot()` 末尾追加焕 emotional_state 回写：
```python
try:
    self.identity_service.update_emotional_state(
        self.user_id, "huan",
        {
            "last_analysis": {
                "primary_emotion": emotion_record.primary_emotion,
                "emotion_intensity": emotion_record.emotion_intensity,
                "deep_need": emotion_record.deep_need,
                "risk_level": emotion_record.risk_level,
            },
            "updated_at": datetime.now().isoformat(),
        }
    )
except Exception:
    pass
```

2. 在 `_persist_emotion_snapshot()` 中将 `full_analysis` 传递给 `update_from_emotion()`：
```python
self.profile_service.update_from_emotion(self.user_id, {
    "primary_emotion": emotion_record.primary_emotion,
    "emotion_intensity": emotion_record.emotion_intensity,
    "deep_need": emotion_record.deep_need,
    "risk_level": emotion_record.risk_level,
    "full_analysis": emotion_record.full_analysis,  # 新增
})
```

3. 在 `profile_service.py` `update_from_emotion()` 中，从 `full_analysis.psychological_traits` 提取性格特征写入 personality_traits（2-6字中文词，每次最多3个，保留最近15个）

## Task 3: 实现身份自动迭代机制（激活 AI_IDENTITY_EVOLVE_PROMPT）

**覆盖机制**: #3, #4

**目标**：激活 `AI_IDENTITY_EVOLVE_PROMPT`，让所有 5 个 AI 基于交互历史自我迭代，填充 `evolution_log`

**修改文件**：
- `backend/app/services/identity_service.py` — 新增 `auto_evolve()` 异步方法
- `backend/app/ai/agent.py` — 在 finally 块中低频条件触发
- `backend/app/services/scheduler.py` — 新增每日身份迭代评估定时任务

**具体改动**：

1. **identity_service.py** 新增 `auto_evolve()` 方法（使用已定义的 `AI_IDENTITY_EVOLVE_PROMPT`）：
```python
async def auto_evolve(self, user_id: int, ai_name: str, 
                      recent_summary: str, user_setting=None) -> bool:
    """基于近期交互，评估并执行身份迭代。使用 AI_IDENTITY_EVOLVE_PROMPT。"""
    identity = self.get_or_create_identity(user_id, ai_name)
    current_json = {
        "gender": identity.gender,
        "personality": identity.personality,
        "speaking_style": identity.speaking_style,
        "values": identity.values,
        "appearance": identity.appearance,
    }
    prompt = AI_IDENTITY_EVOLVE_PROMPT.format(
        current_identity_json=json.dumps(current_json, ensure_ascii=False),
        recent_interaction_summary=recent_summary,
    )
    try:
        llm = get_chat_llm_client(user_setting)
        response, _ = await llm.chat(
            [{"role": "user", "content": prompt}], temperature=0.5
        )
        data = self._parse_json(response)
        if not data or not data.get("changes"):
            return False
        for change in data["changes"]:
            field = change.get("field")
            new_value = change.get("new_value")
            reason = change.get("reason", "auto_evolve")
            if field and new_value:
                self.evolve_field(user_id, ai_name, field, reason, new_value)
        return True
    except Exception as e:
        logger.warning(f"身份自动迭代失败 ({ai_name}): {e}")
        return False
```

2. **agent.py** finally 块中，低频触发（conversation.message_count % 20 == 0）：
```python
# 低频触发身份迭代评估（每20轮对话一次）
try:
    _conv_count = getattr(conv, 'message_count', 0)
    if _conv_count > 0 and _conv_count % 20 == 0:
        _recent_msgs = self.conversation_service.get_recent_messages_for_context(self.user_id, limit=20)
        _summary = "\n".join([f"[{m.role}] {m.content[:100]}" for m in _recent_msgs[-10:]])
        for _ai in ["xuan", "huan", "yao", "ji", "qing"]:
            asyncio.create_task(
                self.identity_service.auto_evolve(self.user_id, _ai, _summary, self.user_setting)
            )
except Exception:
    pass
```

3. **scheduler.py** 新增 `_run_identity_evolution()` 每日 02:00 执行：
```python
async def _run_identity_evolution():
    """每日评估所有用户的 AI 身份迭代。"""
    # 遍历所有有 ai_identities 记录的 user_id
    # 获取近 3 天的对话摘要
    # 为每个 AI 调用 auto_evolve()
```
在 `_scheduler_loop()` 的 02:00 位置添加调用。

## Task 4: 补全 user_profiles 全部缺失字段

**覆盖机制**: #6, #7, #9, #10

**目标**：实现 `attachment_style`、`safe_topics` 的写入逻辑；增强 `summary_text` 定期汇总；激活 safe_topics → 规则引擎的联动

**修改文件**：
- `backend/app/services/profile_service.py`

**具体改动**：

### 4a. attachment_style 规则推断
在 `update_from_emotion()` 中，当 `emotional_baseline` 累计 >= 10 次时，基于情绪分布推断：
```python
def _infer_attachment_style(self, baseline: dict) -> str | None:
    total = sum(baseline.values())
    if total < 10:
        return None
    anxious_ratio = (baseline.get("anxious", 0) + baseline.get("lonely", 0) + baseline.get("stressed", 0)) / total
    avoidant_ratio = (baseline.get("angry", 0) + baseline.get("neutral", 0)) / total
    if anxious_ratio > 0.35:
        return "anxious"
    if avoidant_ratio > 0.4:
        return "avoidant"
    return "secure"
```
在 `update_from_emotion()` 末尾调用，有结果则写入 `profile.attachment_style`。

### 4b. safe_topics 对称填充
在 `update_from_emotion()` 中，当 `emotion_intensity == "light"` 时，将 `primary_emotion` 对应的话题场景加入 safe_topics（与 trigger_topics 的 "high" 逻辑对称）：
```python
if emotion_data.get("emotion_intensity") == "light":
    safe = self._load_json_list(profile.safe_topics)
    topic = emotion_data.get("primary_emotion", "")
    if topic and topic not in safe:
        safe.append(topic)
        profile.safe_topics = json.dumps(safe[-10:], ensure_ascii=False)
```
这将激活 `agent.py` `_sync_user_habits_to_rules()` L1050-1053 中已有的 safe_topics 同步逻辑以及 `_fast_classify()` L1135-1137 中的 `is_safe_topic` 判断。

### 4c. summary_text 定期 LLM 汇总
新增 `generate_summary()` 异步方法，将已有的 personality_traits、interests、core_needs、attachment_style 等字段汇总为自然语言 summary_text：
```python
async def generate_summary(self, user_id: int, user_setting=None):
    profile = self.get_or_create_profile(user_id)
    # 收集所有非空字段 → 构建 prompt → LLM 生成 100 字以内的综合画像摘要
    # 写入 profile.summary_text
```
在 `agent.py` finally 块中，每 10 轮对话触发一次（与 auto_evolve 错开频率）。

## Task 5: 验证

验证所有修改：
- 语法正确性（Python import、类型标注）
- 交叉调用一致性（agent.py ↔ identity_service ↔ profile_service ↔ scheduler）
- 字段写入覆盖率（所有 user_profiles 和 ai_identities 字段都有写入路径）

## 关于 source_summary_ids

`memory_summaries.source_summary_ids` 为空是**设计预期行为**：
- daily 摘要源自消息表，不存在源摘要 ID
- weekly/monthly/yearly 压缩时正确填充 source_summary_ids
- daily 用 `source_message_count` 追踪来源

**无需修复**。

## 执行顺序

```
Task 1 (persona_text + 同步)  ──┐
                                 ├──→ Task 3 (auto_evolve + scheduler) ──→ Task 5 (验证)
Task 2 (emotional_state + traits) ──┘
Task 4 (profile 缺失字段)  ──────────────────────────────────────────────→ Task 5 (验证)
```

- Task 1 和 Task 2 可并行（不同文件区域）
- Task 3 依赖 Task 1（需要完整身份数据才有迭代基础）
- Task 4 独立
- Task 5 在所有实现完成后执行

## 涉及文件汇总

| 文件 | Task | 改动类型 |
|------|------|----------|
| `.qoder/skills/xuanji-xuan/` | 1 | 新建 SKILL.md + persona.md + evolution-rules.md |
| `.qoder/skills/xuanji-huan/` | 1 | 从 psychological-emotion-analyst 重命名 + 新增 persona.md + evolution-rules.md |
| `.qoder/skills/xuanji-yao/` | 1 | 新建 SKILL.md + persona.md + evolution-rules.md |
| `.qoder/skills/xuanji-ji/` | 1 | 新建 SKILL.md + persona.md + evolution-rules.md |
| `.qoder/skills/xuanji-qing/` | 1 | 新建 SKILL.md + persona.md + evolution-rules.md |
| `backend/app/services/identity_service.py` | 1, 3 | 动态加载 + seed_defaults 重构 + auto_evolve 新方法 |
| `backend/app/services/emotion_service.py` | 1 | 路径从 psychological-emotion-analyst 改为 xuanji-huan |
| `backend/app/ai/prompts.py` | 1 | 删除 persona 硬编码，保留模板常量 |
| `backend/app/ai/agent.py` | 1, 2, 3, 4c | persona 加载改为动态 + _persist_emotion_snapshot + finally 触发 |
| `backend/app/services/profile_service.py` | 2, 4 | update_from_emotion 增强 + attachment_style/safe_topics + generate_summary |
| `backend/app/services/scheduler.py` | 3 | 新增 _run_identity_evolution 定时任务 |

## 数据库注意事项

所有修改需重启后端生效。已有用户需删除 `ai_identities` 表旧记录重新触发 `seed_defaults()` 才能应用新的 persona_text 和 is_base_locked。

## 迁移注意事项

- 重命名 `.qoder/skills/psychological-emotion-analyst/` 为 `.qoder/skills/xuanji-huan/` 后，需同步更新 `emotion_service.py` 中的路径引用
- `prompts.py` 中 `XUAN_DEFAULT_PERSONA` 和 `SIMPLE_CHAT_PERSONA` 删除后，`agent.py` 中的引用需改为动态加载
- Skill 文件使用 UTF-8 编码，代码加载时需指定 `encoding="utf-8"`
